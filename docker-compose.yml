services:
  db:
    container_name: postgres
    image: pgvector/pgvector:pg16
    stdin_open: true
    tty: true
    volumes:
      - ./data-pjt/postgres_data:/var/lib/postgresql/data
      - ./data-pjt/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 20s
    env_file:
      - .env
    ports:
      - "${DB_PORT:-5432}:5432"
    networks:
      - de_net

  backend:
    build:
      context: ./backend-pjt
      dockerfile: Dockerfile.dev
    restart: always
    stdin_open: true
    tty: true
    volumes:
      - ./backend-pjt:/app
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    env_file:
      - .env
    command: python manage.py runserver 0.0.0.0:8000
    depends_on:
      db:
        condition: service_healthy
    networks:
      - de_net
      - web_net

  frontend:
    build:
      context: ./frontend-pjt
      dockerfile: Dockerfile.dev
    stdin_open: true
    tty: true
    volumes:
      - ./frontend-pjt:/app
      - /app/node_modules
    ports:
      - "${FRONTEND_PORT:-5173}:5173"
    command: npm run dev -- --host
    depends_on:
      - backend
    networks:
      - web_net

networks:
  de_net:
    driver: bridge
  web_net:
    driver: bridge
