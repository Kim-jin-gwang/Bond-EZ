services:
  frontend:
    build:
      context: ./frontend-pjt
      dockerfile: Dockerfile.dev
    volumes:
      - ./frontend-pjt:/app
      - /app/node_modules
    ports:
      - "${FRONTEND_PORT:-5173}:5173"
    command: npm run dev -- --host
    depends_on:
      - backend
